// Archivo de configuración de reglas ESLint

{
  "rules": {
    // Override our default setting just for this directory
    "eqeqeq": "warn",
    "strict": "off",
    "quotes": ["error", "double"],
    "semi": ["error", "double"],
    "no-dupe-keys": "error"
  }
}